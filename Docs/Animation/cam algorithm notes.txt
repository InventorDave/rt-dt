UPPER, LOWER (for [u,v] pairings, U = 1, L = 0)
MIDDLE = mid(UPPER - LOWER) = (UPPER+LOWER)/2

Try INPUT = MIDDLE. IF OUTPUT < INPUT, UPPER = MIDDLE,
			ELSEIF OUTPUT > INPUT, LOWER = MIDDLE

TRY AGAIN UNTIL RESULT == eq(INPUT, OUTPUT)

